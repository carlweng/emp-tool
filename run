#!/bin/bash
# Two-party local test harness. Pick a fresh random port per invocation so back-
# to-back ctest runs don't collide on a socket still in TIME_WAIT.
PORT=$(( (RANDOM % 30000) + 20000 ))
(sleep 0.05; $1 1 $PORT ${@:2}) &  $1 2 $PORT ${@:2}
